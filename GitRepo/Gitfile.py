#GIT ALL COMMANDS
#Here are the Git commands which are being covered:

git config
git init
git clone
git add
git add -A
git rm --cached filename "This command will Move your file again into Unstaged Area"
git commit "This command will Move your file from Untracked to staged Area"
git commit -a -m "This Cmd will skipp the Stageing Area and commit directly"
git diff
git reset
git status
git rm
git log
git show
git tag
git branch
git checkout
git merge
git remote
git push
git pull
git stash
